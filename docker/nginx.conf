server {
    # 1. Escucha en el puerto 80 (El único puerto público para Render)
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    # Raíz para archivos estáticos de Laravel
    root /var/www/public;
    index index.php index.html;

    # Optimización para subida de archivos
    client_max_body_size 100M;
    charset utf-8;

    # Logs a la salida estándar
    access_log /dev/stdout;
    error_log /dev/stderr warn;

    # Headers de seguridad
    server_tokens off;
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header X-XSS-Protection "1; mode=block";

    # Compresión Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # =========================================================
    # EXCEPCIÓN CRÍTICA: LOGIN (Manejado por Astro)
    # =========================================================
    # Esta regla va ANTES que la general de Laravel.
    # Le dice a Nginx: "El login mándaselo a Astro (3002), no a Laravel".
    location = /api/auth/login {
        proxy_pass http://127.0.0.1:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Headers para que Astro sepa la IP real
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # =========================================================
    # REGLA 1: LARAVEL (Backend API & Admin)
    # =========================================================
    # Todo lo demás que empiece con /api, /storage o /admin va a Laravel
    location ~ ^/(api|storage|admin) {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Procesamiento de PHP (Laravel) en el puerto interno 9000
    location ~ \.php$ {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_param HTTPS 'on';
    }

    # =========================================================
    # REGLA 2: ASTRO (Frontend SSR)
    # =========================================================
    # Todo lo demás se envía al servidor de Astro en el puerto 3002
    location / {
        proxy_pass http://127.0.0.1:3002;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Bloqueo de seguridad
    location ~ /\.(?!well-known).* {
        deny all;
    }
}