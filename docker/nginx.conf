server {
    # 1. Nginx escucha en el Puerto 80 (El único puerto público)
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    # Raíz para archivos estáticos de Laravel
    root /var/www/public;
    index index.php index.html;

    # Optimización para subida de archivos (ERP)
    client_max_body_size 100M;
    charset utf-8;

    # Logs a la salida estándar
    access_log /dev/stdout;
    error_log /dev/stderr warn;

    # Headers de seguridad
    server_tokens off;
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header X-XSS-Protection "1; mode=block";

    # Compresión Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # =========================================================
    # REGLA 1: LARAVEL (Backend API & Admin)
    # =========================================================
    # Todo lo que empiece con /api, /storage o /admin se procesa aquí
    location ~ ^/(api|storage|admin) {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Procesamiento de PHP (Laravel) en el puerto interno 9000
    location ~ \.php$ {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        
        # Corrección para HTTPS en Render
        fastcgi_param HTTPS 'on';
    }

    # =========================================================
    # REGLA 2: ASTRO (Frontend SSR)
    # =========================================================
    # Todo lo demás se envía al servidor de Astro en el puerto 3002
    location / {
        proxy_pass http://127.0.0.1:3002;
        
        # Headers vitales para SSR y WebSockets
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Bloqueo de seguridad
    location ~ /\.(?!well-known).* {
        deny all;
    }
}